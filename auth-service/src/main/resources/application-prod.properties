# Production configuration for Auth Service
spring.application.name=auth-service
server.port=5000

# Production Database - MySQL
spring.datasource.url=jdbc:mysql://13.54.216.34:3306/84pos_auth?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
spring.datasource.username=84pos_user
spring.datasource.password=84pos_password_2024
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration for Production
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.properties.hibernate.format_sql=false

# Logging for Production
logging.config=classpath:log4j2-spring.xml
logging.level.anhlv.auth=INFO
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=WARN

# JWT Configuration
jwt.secret=84pos_jwt_secret_key_2024_production_very_long_and_secure
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=*
cors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS
cors.allowed.headers=*

# OpenTelemetry Configuration
otel.service.name=auth-service
otel.resource.attributes=service.name=auth-service,service.version=1.0.0
